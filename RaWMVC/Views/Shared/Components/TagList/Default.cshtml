@model List<RaWMVC.Data.Entities.Tag>
@{
	var page = (int)ViewBag.Page;
	var maxPage = (int)ViewBag.MaxPage;
	// var previousPage = page - 1;
	var previousPage = page == 1 ? page : page - 1;
	// var nextPage = page + 1;
	var nextPage = page == maxPage ? maxPage : page + 1;
}

<div class="row">
	<div class="col-12">
		<div class="card">
			<div class="card-content collapse show">
				<div class="table-responsive">
					<table class="table">
						<thead class="thead-dark">
							<tr>
								<th scope="col">#</th>
								<th scope="col">Name</th>
								<th scope="col">Description</th>
								<th scope="col">Action</th>
							</tr>
						</thead>
						<tbody>
							@if (Model != null)
							{
								foreach (var tag in Model)
								{
									<tr>
										<th scope="row">@tag.Position</th>
										<td>@tag.tagName</td>
										<td>@tag.tagDescription</td>
										<td>
											<a class="fs-4" href="~/Tag/Edit/?idTag=@tag.tagId">
												<i class="fa-solid fa-file-pen"></i>
											</a>
											<button class="btn btn-outline-primary" type="button" onclick="deleteTag('@tag.tagId')">
												<i class="fa-solid fa-eraser"></i>
											</button>
										</td>
									</tr>
								}
							}
							else
							{
								<tr>
									<td colspan="3">Empty data</td>
								</tr>
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>
<div class="row" style="margin-bottom: 20px;">
@* 	<div class="col-md-12">
		@if(previousPage == 0)
		{
			<span class="span_paging" style="background-color: gray">Previous</span>
		} else
		{
			<span class="span_paging" onclick="getData(@previousPage)">Previous</span>
		}
		@{
			var onclickEvent = nextPage == maxPage ? "style='background-color: gray'" : "onclick=getData(" + nextPage + ")";
		}
		<span class="span_paging" onclick="getData(@nextPage)">Next</span>
		<span class="span_paging" @onclickEvent>Next</span>
	</div> *@
	<nav aria-label="Page navigation example">
		<ul class="pagination justify-content-center pagination-lg">
			@if (previousPage == 0)
			{
				<li class="page-item disabled">
					<a class="page-link" href="#" aria-label="Previous">
						<span aria-hidden="true">&laquo;</span>
					</a>
				</li>
			}
			else
			{
				<li class="page-item" onclick="getData(@previousPage)">
					<a class="page-link" href="#" aria-label="Previous">
						<span aria-hidden="true">&laquo;</span>
					</a>
				</li>
			}
@* 			<li class="page-item"><a class="page-link" href="#">1</a></li>
			<li class="page-item"><a class="page-link" href="#">2</a></li>
			<li class="page-item"><a class="page-link" href="#">3</a></li> *@
@* 			@{
				var onclickEvent = nextPage == maxPage ? "class='disabled page-item'" : "onclick=getData(" + nextPage + ")";
			} *@
			<li class="page-item" onclick="getData(@nextPage)">
				<a class="page-link" href="#" aria-label="Next">
					<span aria-hidden="true">&raquo;</span>
				</a>
			</li>
		</ul>
	</nav>
</div>